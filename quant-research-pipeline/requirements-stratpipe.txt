# ── Strategy Development Environment ──
# Clean, lightweight env for research code, backtesting, and notebooks.
# Install: python3 -m venv .venv-stratpipe && .venv-stratpipe/bin/pip install -r requirements-stratpipe.txt
# Usage:  source .venv-stratpipe/bin/activate

# ── Data & Computation ──
pandas>=2.2,<3.0
numpy>=1.26,<3.0
scipy>=1.13,<2.0
statsmodels>=0.14,<1.0
scikit-learn>=1.5,<2.0

# ── Visualisation ──
matplotlib>=3.9,<4.0
seaborn>=0.13,<1.0

# ── Testing ──
pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0
hypothesis>=6.100,<7.0

# ── Notebooks ──
jupyterlab>=4.2,<5.0
nbformat>=5.10,<6.0
ipykernel>=6.29,<7.0

# ── Code Quality ──
ruff>=0.5,<1.0
pre-commit>=3.7,<4.0

# ── Pipeline Tooling ──
pyyaml>=6.0,<7.0
pydantic>=2.0,<3.0
typer>=0.12,<1.0
rich>=13.0,<14.0

# ── Build & Packaging ──
build>=1.2,<2.0
